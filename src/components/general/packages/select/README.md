# 思路
- 组件不对原生select和option进行原生封装，以为下拉框的拓展功能很多，例如输入搜索，自定义下拉选项等

## 子类
- 显示值和输入逻辑
- 下拉内容


## 子类1
1、结构
- 只读的输入框
- 自定义input输入框

2、表现
- 禁用

3、行为
- 输入
- 点击toggle下拉
- 输入和子类2进行联系，发布订阅（输入debounce，是否可以抽象debounce用在很多地方）

### 子类1拓展
- 单选 -》 多选，多选用tag显示
- 下拉 -》 关闭下拉也可以在组件外触发

## 子类2
1、结构

2、表现
- poper位置
- 内容滚动

3、行为
- 选中和子类1进行联系，发布订阅


# 问题
1、自定义指令中binding的expression在绑定的值为函数、布尔值、字符串时是怎样的？

2、了解Vnode

# 注意点
1、click-outside指令要注意判断，mousedown和mouseup的target是否在元素上，以及要处理多个下拉组件展开的时候一起关闭。（发布订阅）
